import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import SwaggerApiList from './App';

ReactDOM.render(
  <React.StrictMode>
    <SwaggerApiList />
  </React.StrictMode>,
  document.getElementById('root')
);
